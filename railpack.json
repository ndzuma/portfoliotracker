{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "22"
  },
  "steps": {
    "build": {
      "commands": ["...", "npx convex deploy && pnpm run build"]
    }
  },
  "deploy": {
    "startCommand": "pnpm run start"
  }
}
