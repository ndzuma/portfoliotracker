[build]
builder = "nixpacks"
buildCommand = "npm install && npx convex deploy --cmd 'npm run build'"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "never"

[phases.setup]
nixPkgs = ["nodejs_18"]

[install]
cmds = ["npm install"]
